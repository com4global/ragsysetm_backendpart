[build]
  command = "pip install -r requirements.txt"
  functions = "functions"

[dev]
  command = "uvicorn main:app --reload"

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/netlify_app"
  status = 200
